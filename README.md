## Выгрузка минимальной и макисальной цены на товар

изменил названия таблиц с единичного на множесственный и убрал дублирование имени таблицы в полях для более laravel-way структуры бд

также убрал таблицы для статусов ввиду их малого количества

файлы хранятся по пути ```storage/app/reports/```

шаги для поднятия проекта:
````
cp .env.example .env
php artisan migrate
php artisan optimize
php artisan queue:listen // в отдельном окне
php artisan queue:serve  // в отдельном окне
````
!!! проект хотел развернуть на докере с раздлением по батчам. Пагинацией на индексе, использованием фасада Storage, но столкунулся с проблемами и решил самым простым способом.  В будущем реализую как хотел не зависимо от проверки решения
